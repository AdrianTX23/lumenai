/// LUMEN AI domain layer: pure Dart business model.
///
/// This package holds entities, value objects, repository interfaces (ports),
/// use cases and domain services. It depends on nothing — no Flutter, no IO,
/// no serialization. See `docs/02-architecture.md` §3.
library;

export 'src/failures/failure.dart';
export 'src/failures/result.dart';
